<ul>
    <% articles.map((article,i)=> { %>
        <li>
            <p>
                <%= i+1%>
            </p>
            <h2>
                <%= article.title %>
            </h2>
            <p>
                <%= article.body %>
            </p>
        </li>
        <hr />
    <% }) %>
</ul>